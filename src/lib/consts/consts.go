package consts

//NOTE: Any dir path must end with "/"
const(
        DIR_CFG = "../share/etc/"
        DIR_HTML = "../share/html/"
        DIR_JS = "../share/js/"
        DIR_CSS = "../share/css/"
        DIR_MAKE = "../share/make/"
        DIR_LANG = "../share/lang/"
        DIR_LOG = "../share/log/"
        CMD_DEPLOY = "../share/libexec/deploy.sh"
        CFG_SESSION_TIMEOUT = 3600  //1 hour
)
