#!/bin/sh

GOPATH=$(pwd)
echo -e "all: build\n\nbuild:\n\texport GOPATH=${GOPATH}\n\tmkdir -p ${GOPATH}/bin\n\tgo get github.com/tonychee7000/mysql\n\tgo build glvsadm\n\tmv glvsadm ${GOPATH}/bin" > Makefile
